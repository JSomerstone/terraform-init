[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]
pytest = "*"
pytest-cov = "*"

[packages]

[requires]
python_version = "3.7"

[scripts]
test = "pytest -v test/"
"test:coverage" = "pytest --cov=src/ --cov-branch --cov-report=html test/"
